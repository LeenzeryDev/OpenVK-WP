<Page
    x:Class="OpenVK.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenVK"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        
        <StackPanel Height="50" Background="{ThemeResource ThemeBackground}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10,0,10,10">
                <Button x:Name="newsButton" Margin="0,0,10,0" Content="&#x1f4dc; " FontFamily="Segoe UI Symbol" Background="{x:Null}" BorderBrush="{x:Null}" FontSize="24"/>
                <Button x:Name="messagesButton" Margin="0,0,10,0" Content="&#xE8BD;" FontFamily="Segoe MDL2 Assets" Background="{x:Null}" BorderBrush="{x:Null}" FontSize="24"/>
                <Button x:Name="profileButton" Margin="0,0,10,0" Content="&#xE8D4;" FontFamily="Segoe MDL2 Assets" Background="{x:Null}" BorderBrush="{x:Null}" FontSize="24"/>
            </StackPanel>
        </StackPanel>
        <TextBlock x:Name="textBlock" HorizontalAlignment="Center" TextWrapping="Wrap" Text="Здесь ничего нет, пока что..." VerticalAlignment="Center" FontSize="24" FontFamily="Segoe UI Light"/>
    </Grid>
</Page>
